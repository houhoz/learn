// tuple -> 元组
const teacherInfo: [string, string, number] = ['name', 'male', 18]
// enum -> 枚举

enum Direction {
  Up = 100,
  Down,
  Left,
  Right,
}

enum Gender {
  Mail,
  Female,
}

console.log('Direction :>> ', Direction.Down)
